# Copy to docker/.env and set your LAN IPv4 if WebRTC fails on localhost (common with Firefox).
# Example: LIVEKIT_NODE_IP=192.168.3.150
# Then open the web app at http://192.168.3.150:5173 (not localhost).
LIVEKIT_NODE_IP=127.0.0.1
